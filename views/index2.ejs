<%-include("./partials/header") %> 
<%-include("./partials/navbar") %> 



<div
  class="
    home-container
    container
    mt-4
    animate__animated animate__fadeIn animate__slow 
  "
>

<div class="row">
  <h1 class="text-center" style="font-weight:900 ;color:red">Our Customers</h1>
</div>

  <% if (customers.length>0) { %>
  
  <div class="row">
    <div class="col">
      <table
        class="
          customers-table
          table table-primary table-striped table-bordered
          border-dark
          mt-4
        "
      >
        <thead class="text-center fs-6" style="background-color: brown;">
          <tr style="color: blue;">
            <th>Sr. No.</th>
            <th>Name</th>
            <th>Email ID</th>
            <th>Address</th>
            <th>Current Balance</th>
            <th>Details</th>
          </tr>
        </thead>
        <tbody class="text-center fs-6">
          <% customers.forEach((customer,index) => { %>
          <tr>
            <td class="fw-bold"><%=index +1%></td>
            <td><%=customer.name%></td>
            <td><%=customer.email%></td>
            <td><%=customer.address%></td>
            <td>
              <span><i class="fas fa-rupee-sign"></i></span
              ><%=(customer.currentBal).toLocaleString('en-IN')%>
            </td>
            <td>
              <a
                type="button"
                class="btn btn-sm btn-primary p-1 fw-bold"
                href="/customers/<%=(customer.accNo).toString()%>"
              >
                View Details
              </a>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>
  <% }else{ %>
  <div class="row">
    <h1 class="text-center">There are no customers at the moment!</h1>
  </div>
  <%}%>
</div>
